import React, { FC } from 'react'
import { Button } from 'antd'
import './index.less'

const Header: FC = () => {
  return (
    <header>
      test header<Button>按钮</Button>
    </header>
  )
}

export default Header
